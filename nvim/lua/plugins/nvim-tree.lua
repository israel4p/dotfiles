local cmd = vim.cmd

require('nvim-tree').setup {
--  open_on_setup = false,
--  view = {
--      width = 32,
--      auto_resize = true
--  },
--  filters = {
--      dotfiles = true,
--      custom = { '.git', 'node_modules', '.cache', '.bin' },
--  },
}

